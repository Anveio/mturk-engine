import { Classes } from '@blueprintjs/core';

export const SMALL_MINIMAL_BUTTON =
  Classes.BUTTON + ' ' + Classes.SMALL + ' ' + Classes.MINIMAL;

export const MINIMAL_BUTTON_GROUP = `${Classes.BUTTON_GROUP} ${
  Classes.MINIMAL
}`;

export const CONDENSED_TABLE = `${Classes.HTML_TABLE} ${Classes.SMALL}`;
